name: LinkedIn Monitor

permissions:
  contents: write
  actions: read

on:
  schedule:
    - cron: '0 8,16 * * *'  # 8h et 16h UTC
  workflow_dispatch:
    inputs:
      force_run:
        description: 'Forcer l exécution'
        required: false
        default: 'false'
        type: boolean

env:
  PYTHON_VERSION: '3.11'

jobs:
  linkedin-monitor:
    name: 🔔 LinkedIn Alerts
    runs-on: ubuntu-latest
    timeout-minutes: 20
    
    steps:
    - name: 📥 Checkout code
      uses: actions/checkout@v4
      with
